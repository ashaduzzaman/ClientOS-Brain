# MCPize Configuration
# Docs: https://mcpize.com/docs/mcpize-yaml

version: 1

name: "clientos-brain"
slug: "clientos-brain"

runtime: typescript

build:
  install: "npm install"
  command: "npm run build"
